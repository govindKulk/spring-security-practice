package com.example.security;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Basic test to verify that the Spring Security application context loads successfully.
 * 
 * This test ensures that:
 * - Spring Boot application starts correctly
 * - Spring Security configuration is valid
 * - All beans are created successfully
 */
@SpringBootTest
class SecurityApplicationTests {

    @Test
    void contextLoads() {
        // This test verifies that the Spring application context loads successfully
        // If there are any configuration issues, this test will fail
    }
} 